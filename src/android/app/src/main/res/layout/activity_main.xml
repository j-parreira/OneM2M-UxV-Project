<?xml version="1.0" encoding="utf-8"?>
<!--
    activity_main.xml — Ecrã de registo SDK e lançamento do benchmark.

    Contém:
    - Barra de progresso para download da fly zone DB
    - Estado de conexão do drone
    - Botão de registo DJI SDK
    - Botão para abrir o ecrã de controlo de voo (FlightActivity)
    - Versão do SDK (debug)
-->
<com.dji.sdk.duvops.app.MainContent
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_content"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clickable="true"
    android:orientation="vertical"
    tools:context=".app.MainActivity">

    <!-- Progresso do download da fly zone DB (horizontal, 5dp de altura) -->
    <ProgressBar
        android:id="@+id/progress_bar"
        style="@android:style/Widget.ProgressBar.Horizontal"
        android:layout_width="match_parent"
        android:layout_height="5dp"
        android:max="100"
        android:progress="0"
        android:visibility="gone" />

    <ScrollView
        android:id="@+id/startup_scroll_root"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#000000"
        android:fillViewport="true">

        <LinearLayout
            android:id="@+id/startup_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center"
            android:padding="24dp">

            <!-- Estado de conexão do drone -->
            <TextView
                android:id="@+id/text_connection_status"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Aguardando ligação..."
                android:textColor="@android:color/white"
                android:textSize="18sp"
                android:textStyle="bold"
                android:gravity="center"
                android:layout_marginBottom="12dp" />

            <!-- Modelo do drone -->
            <TextView
                android:id="@+id/text_product_info"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Produto desconhecido"
                android:textColor="@android:color/darker_gray"
                android:textSize="14sp"
                android:gravity="center"
                android:layout_marginBottom="32dp" />

            <!-- Registo do SDK DJI -->
            <Button
                android:id="@+id/btn_registerApp"
                android:layout_width="220dp"
                android:layout_height="50dp"
                android:layout_marginTop="8dp"
                android:background="@android:drawable/btn_default"
                android:text="Registar App"
                android:textAllCaps="false" />

            <!-- Abrir ecrã de controlo e benchmark -->
            <Button
                android:id="@+id/btn_open"
                android:layout_width="220dp"
                android:layout_height="60dp"
                android:layout_marginTop="32dp"
                android:text="Open Benchmark"
                android:textStyle="bold"
                android:textSize="16sp"
                android:theme="@style/ThemeOverlay.AppCompat.Dark"
                android:backgroundTint="@android:color/holo_blue_dark"
                android:textColor="@android:color/white" />

            <!-- Versão do SDK (debug info) -->
            <TextView
                android:id="@+id/text_version"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="48dp"
                android:text="SDK Version"
                android:textColor="#666666"
                android:textSize="12sp" />

        </LinearLayout>
    </ScrollView>
</com.dji.sdk.duvops.app.MainContent>
